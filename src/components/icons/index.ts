// Ícones SVG puros (sem cor fixa) — usam stroke="currentColor", então a cor
// vem da classe de texto (text-primary-light, text-accent etc.) aplicada por
// quem usa o componente, no `className`. Tamanho também é definido por quem
// usa (ex. "h-5 w-5"), não pelos ícones.
export * from "./activity";
export * from "./social";
